interface:
  display_name: "学术证据通道"
  short_description: "形成用于领域判断的本地学术资料通道"
  default_prompt: "Use $academic-evidence-lane to build an academic evidence lane from my local sources."
policy:
  allow_implicit_invocation: false
