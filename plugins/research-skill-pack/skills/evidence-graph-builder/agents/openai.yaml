interface:
  display_name: "证据图谱构建"
  short_description: "维护 supports、contradicts、extends、replicates 等可追溯证据关系。"
  default_prompt: "Use $evidence-graph-builder for its traceable research workflow in my local project."
policy:
  allow_implicit_invocation: false

