interface:
  display_name: "公开数据提供方路由"
  short_description: "为已定义的数据需求检查已配置 provider；未配置时返回 not_configured。"
  default_prompt: "Use $public-data-provider-router for its traceable research workflow in my local project."
policy:
  allow_implicit_invocation: false

