interface:
  display_name: "时间基线构建"
  short_description: "形成选题前需确认的时间与容量基线"
  default_prompt: "Use $timeline-baseline-builder to build my research timeline baseline."
policy:
  allow_implicit_invocation: false
